/**
 * EXCHANGE Enum
 */
export enum EXCHANGE {
  CREATED = 'member.created.exchange',
  UPDATED = 'member.updated.exchange',
  DELETED = 'member.deleted.exchange',
}

/**
 * QUEUE Enum
 */
export enum QUEUE {
  CREATED = 'member.created.queue',
  UPDATED = 'member.updated.queue',
  DELETED = 'member.deleted.queue',
}

/**
 * KEY Enum
 */
export enum KEY {
  CREATED = 'member.created.key',
  UPDATED = 'member.updated.key',
  DELETED = 'member.deleted.key',
}
