/**
 * EXCHANGE Enum
 */
 export enum EXCHANGE {
  CREATED = 'report.created.exchange',
  UPDATED = 'report.updated.exchange',
  DELETED = 'report.deleted.exchange',
}

/**
 * QUEUE Enum
 */
export enum QUEUE {
  CREATED = 'report.created.queue',
  UPDATED = 'report.updated.queue',
  DELETED = 'report.deleted.queue',
}

/**
 * KEY Enum
 */
export enum KEY {
  CREATED = 'report.created.key',
  UPDATED = 'report.updated.key',
  DELETED = 'report.deleted.key',
}
