/**
 * Report Populate Enum
 */
export enum ReportPopulateEnum {
  REPORTER = `reporter`,
  CONVERSATION = `conversation`,
  USER = `user`,
  CONVERSATION_MEMBERS = `conversation.members`,
  CONVERSATION_AUTHOR = `conversation.author`,
}
