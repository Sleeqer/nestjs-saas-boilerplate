/**
 * EXCHANGE Enum
 */
export enum EXCHANGE {
  CREATED = 'profile.created.exchange',
  UPDATED = 'profile.updated.exchange',
  DELETED = 'profile.deleted.exchange',
}

/**
 * QUEUE Enum
 */
export enum QUEUE {
  CREATED = 'profile.created.queue',
  UPDATED = 'profile.updated.queue',
  DELETED = 'profile.deleted.queue',
}

/**
 * KEY Enum
 */
export enum KEY {
  CREATED = 'profile.created.key',
  UPDATED = 'profile.updated.key',
  DELETED = 'profile.deleted.key',
}
