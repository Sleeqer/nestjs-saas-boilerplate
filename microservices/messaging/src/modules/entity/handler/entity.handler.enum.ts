export enum EXCHANGE {
  CREATED = 'entity.created.exchange',
  UPDATED = 'entity.updated.exchange',
  DELETED = 'entity.deleted.exchange',
}

export enum QUEUE {
  CREATED = 'entity.created.queue',
  UPDATED = 'entity.updated.queue',
  DELETED = 'entity.deleted.queue',
}

export enum KEY {
  CREATED = 'entity.created.key',
  UPDATED = 'entity.updated.key',
  DELETED = 'entity.deleted.key',
}
