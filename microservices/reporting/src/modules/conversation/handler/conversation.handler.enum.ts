export enum EXCHANGE {
  CREATED = 'conversation.created.exchange',
  UPDATED = 'conversation.updated.exchange',
  DELETED = 'conversation.deleted.exchange',
}

export enum QUEUE {
  CREATED = 'conversation.created.queue',
  UPDATED = 'conversation.updated.queue',
  DELETED = 'conversation.deleted.queue',
}

export enum KEY {
  CREATED = 'conversation.created.key',
  UPDATED = 'conversation.updated.key',
  DELETED = 'conversation.deleted.key',
}
