export enum EXCHANGE {
  CREATED = 'user.created.exchange',
  UPDATED = 'user.updated.exchange',
  DELETED = 'user.deleted.exchange',
}

export enum QUEUE {
  CREATED = 'user.created.queue',
  UPDATED = 'user.updated.queue',
  DELETED = 'user.deleted.queue',
}

export enum KEY {
  CREATED = 'user.created.key',
  UPDATED = 'user.updated.key',
  DELETED = 'user.deleted.key',
}
