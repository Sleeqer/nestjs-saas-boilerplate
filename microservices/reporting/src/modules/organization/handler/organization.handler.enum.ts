export enum EXCHANGE {
  CREATED = 'organization.created.exchange',
  UPDATED = 'organization.updated.exchange',
  DELETED = 'organization.deleted.exchange',
}

export enum QUEUE {
  CREATED = 'organization.created.queue',
  UPDATED = 'organization.updated.queue',
  DELETED = 'organization.deleted.queue',
}

export enum KEY {
  CREATED = 'organization.created.key',
  UPDATED = 'organization.updated.key',
  DELETED = 'organization.deleted.key',
}
